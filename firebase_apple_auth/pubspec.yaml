name: firebase_apple_auth
description: A new flutter plugin project.
version: 0.1.0
author: Amr Yousef
homepage: https://github.com/amrfarid140/firebase_apple_auth/firebase_apple_auth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_apple_auth_platform_interface: ^0.1.0
  firebase_auth: ^0.15.3
  firebase_core: ^0.4.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    androidPackage: me.amryousef.apple.auth.firebase_apple_auth
    pluginClass: FirebaseAppleAuthPlugin
