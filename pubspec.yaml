name: firebase_apple_auth
description: A new flutter plugin project.
version: 0.1.0
author: Amr Yousef
homepage: https://github.com/amrfarid140/firebase_apple_auth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    androidPackage: me.amryousef.apple.auth.firebase_apple_auth
    pluginClass: FirebaseAppleAuthPlugin
